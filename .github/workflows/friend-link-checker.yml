name: 🤝 自动更新友链

on:
  issues:
    types: [opened, reopened]
  issue_comment:
    types: [created]

jobs:
  update-friends:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # playwright 仅服务友链脚本，依赖隔离在 .github/scripts 下，不进站点依赖
      - name: Install friend-link script dependencies
        run: pnpm install --frozen-lockfile
        working-directory: .github/scripts

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
        working-directory: .github/scripts

      - name: Process friend request
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const path = require('node:path');
            const handler = require(path.join(process.env.GITHUB_WORKSPACE, '.github/scripts/process-friend-request.cjs'));
            await handler({ github, context, core });
